[workspace]
members = ["crates/kernel"]

[package]
name = "jos"
version = "0.1.0"
edition = "2024"

[dependencies]
# Das Hauptprojekt zieht sich den Kernel als lokale Abhängigkeit
kernel = { path = "crates/kernel" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
